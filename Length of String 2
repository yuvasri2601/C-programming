#include<stdio.h>
int main()
{
    char name[50];
    int len=0;
    printf("Enter the string: ");
    gets(name);
    for(int i=0;name[i]!='\0';i++)
    {
        len++;
    }
    printf("%d",len);
    return 0;
}
