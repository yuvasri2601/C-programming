#include <stdio.h>
int main() {
    int number;
    printf("Enter the number: ");
    scanf("%d",&number);
    if(number%2==0)
    {
        printf("Enter the number is even",number);
    }
    else
    {
        printf("Enter the number is odd",number);
    }
    return 0;
}
