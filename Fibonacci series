#include<stdio.h>
int main()
{
    int n,i,first=0, second=1,temp;
    printf("How many series you want?: ");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        printf("%d ",first);
        temp=first;
        first=second;
        second=temp+second;
    }
