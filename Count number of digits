#include<stdio.h>
int main()
{
    int n,count=0,rem;
    printf("Enter the digits: ");
    scanf("%d",&n);
    while(n>0)
    {
        n/=10;
        count+=1;
    }
    printf("%d",count);
    return 0;
}
