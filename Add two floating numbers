#include <stdio.h>
int main() {
    double num1,num2;
    printf("Enter the two numbers: ");
    scanf("%lf%lf",&num1,&num2);
    printf("Addition of %lf and %lf is: %.2lf",num1,num2,num1+num2);
    return 0;
}
