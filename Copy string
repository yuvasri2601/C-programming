#include<stdio.h>
int main()
{
    char a[100],b[100],i;
    printf("Enter the string: ");
    gets(a);
    for(i=0;a[i]!='\0';i++)
    {
        b[i]=a[i];
    }
    printf("The copied string is:  %s",b);
    return 0;
}
