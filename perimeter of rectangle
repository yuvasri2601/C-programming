#include<stdio.h>
int main()
{
    int n,l,b,perimeter;
    scanf("%d",&n);
    if(n>10)
    {
        l=n%10;
        b=n/10;
        perimeter=l+b;
        printf("%d",2*perimeter);
    }
    else
        printf("-1");
    return 0;
}
